name: onecard_game
description: A multiplayer OneCard game built with Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 상태 관리
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3
  
  # Firebase
  # Firebase (removed -- using local SQLite instead)
  
  # 유틸리티
  equatable: ^2.0.5
  uuid: ^4.2.1
  path_provider: ^2.0.15
  sqflite: ^2.2.7
  sqflite_common_ffi: ^2.3.6
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  riverpod_generator: ^2.3.9
  custom_lint: ^0.7.6
  riverpod_lint: ^2.3.8

flutter:
  uses-material-design: true

